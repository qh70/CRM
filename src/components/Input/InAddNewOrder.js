const InputInAddNewOrder = ({type, value, onChange}) => {
    return (
        <input className="inputInAddNewOrder" type={type} value={value} onChange={onChange}></input>
    )
}

export default InputInAddNewOrder